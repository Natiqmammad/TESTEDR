# db_redis

Simple key-value usage via `forge.db` with a Redis backend.

```
apexrc build
apexrc run
```

Update the connection URL in `src/main.afml` as needed (defaults to `redis://127.0.0.1:6379/`).
