# db_postgres

Showcases `forge.db` with a PostgreSQL backend. Update the connection URL before running.

```
export PG_URL=postgres://postgres:password@localhost:5432/postgres
apexrc build
PG_URL=... apexrc run
```
