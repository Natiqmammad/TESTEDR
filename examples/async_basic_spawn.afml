import forge;
import forge.log as log;
import forge.async as async;

fun worker(x:: i32) -> i32 {
    return x + 1;
}

async fun apex() {
    log.info("[async_basic_spawn] start");
    let f = async.spawn(worker, 41);
    let v = await f;
    log.info("[async_basic_spawn] spawn result = ", v);
}
