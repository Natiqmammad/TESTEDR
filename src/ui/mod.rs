//! UI module for AFNS
//!
//! This module provides UI-related types and utilities.

/// Placeholder for UI types
pub struct UIContext;

impl UIContext {
    pub fn new() -> Self {
        Self
    }
}

impl Default for UIContext {
    fn default() -> Self {
        Self::new()
    }
}
